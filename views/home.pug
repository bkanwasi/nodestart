extends layout

block content
  h1= title
  p Welcome  #{userDetail.f_name}




    each val, index in userDetail
      p= index + ': ' + val
    endeach

        <form action="/upload"  method='post'  encType="multipart/form-data">
           <div class="form-group">
           <label for="exampleFormControlFile1">Example file input</label>
           <input type="file" class="form-control-file" id="exampleFormControlFile1"  name="profile_pic">
           </div>
           <button type="submit" class="btn btn-primary">Upload</button>
             <a href="logout">
              <button type="button" class="btn btn-primary">Logout</button> </a>
        </form> 

          <img src="/images/#{userDetail.profile_pic}" class="img-thumbnail" width="200" height="200"/>
        
            